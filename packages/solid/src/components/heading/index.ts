import { HeadingRoot as Root } from "./heading";

import type {
  HeadingRootProps as RootProps,
  HeadingRootRenderProps as RootRenderProps,
  HeadingRootVariantProps as RootVariantProps,
} from "./heading.props";

export type {
  RootProps,
  RootRenderProps,
  RootVariantProps,
};

export {
  Root,
};

export const Heading = Root;

export default Heading;
