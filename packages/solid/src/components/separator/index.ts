import { SeparatorRoot as Root } from "./separator";

import type {
  SeparatorRootProps as RootProps,
  SeparatorRootRenderProps as RootRenderProps,
  SeparatorRootVariantProps as RootVariantProps,
} from "./separator.props";

export type {
  RootProps,
  RootRenderProps,
  RootVariantProps,
};

export {
  Root,
};

export const Separator = Object.assign(Root);

export default Separator;
