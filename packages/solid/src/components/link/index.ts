import { LinkRoot as Root } from "./link";

import type {
  LinkRootProps as RootProps,
  LinkRootRenderProps as RootRenderProps,
  LinkRootVariantProps as RootVariantProps,
} from "./link.props";

export type {
  RootProps,
  RootRenderProps,
  RootVariantProps,
};

export {
  Root,
};

export const Link = Root;

export default Link;
