import { CursorRoot as Root } from "./cursor";

import type {
  CursorRootProps as RootProps,
  CursorRootRenderProps as RootRenderProps,
  CursorRootVariantProps as RootVariantProps,
} from "./cursor.props";

export type {
  RootProps,
  RootRenderProps,
  RootVariantProps,
};

export {
  Root,
};

export const Cursor = Object.assign(Root);

export default Cursor;
