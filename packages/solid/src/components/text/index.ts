import { TextRoot as Root } from "./text";

import type {
  TextRootProps as RootProps,
  TextRootVariantProps as RootVariantProps,
} from "./text.props";

export type {
  RootProps,
  RootVariantProps,
};

export {
  Root,
};

export const Text = Object.assign(Root);

export default Text;
